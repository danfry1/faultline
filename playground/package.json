{
  "name": "faultline-playground",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npx tsx src/index.ts && echo '' && echo '── ESLint faultline rules ──' && npx eslint src/lint-demo.ts || true",
    "lint": "npx eslint src/"
  },
  "dependencies": {
    "faultline": "^0.1.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "eslint-plugin-faultline": "^0.1.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.0"
  }
}
