{
  "name": "faultline-vscode",
  "displayName": "Faultline",
  "description": "Type-safe error handling diagnostics for TypeScript — surfaces missing error coverage, shows throwable errors on hover, and provides quick fixes.",
  "version": "0.1.0",
  "publisher": "faultline",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": ["Linters", "Programming Languages"],
  "activationEvents": [
    "onLanguage:typescript",
    "onLanguage:typescriptreact"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "Faultline",
      "properties": {
        "faultline.enable": {
          "type": "boolean",
          "default": true,
          "description": "Enable Faultline diagnostics"
        },
        "faultline.analyzeOnSave": {
          "type": "boolean",
          "default": true,
          "description": "Run analysis when files are saved"
        }
      }
    },
    "commands": [
      {
        "command": "faultline.analyze",
        "title": "Faultline: Analyze Project"
      }
    ]
  },
  "scripts": {
    "build": "tsc -p .",
    "watch": "tsc -p . --watch"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "typescript": "^5.9.3"
  }
}
